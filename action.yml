name: 'npm-semantic-publish-action'
description: 'Github action to publish npm packages with semanantic versioning rules'
author: 'iuccio'

# Define your inputs here.
inputs:
  milliseconds:
    description: 'Your input description here'
    required: true
    default: '1000'

# Define your outputs here.
outputs:
  time:
    description: 'Your output description here'

runs:
  using: node20
  main: dist/index.js
